const { Queue } = require('../Queue.js');

describe('Stack Test', () => {
  it('Enqueue should add an item to the queue', () => {
    const myArray = ['AliG', 'Borat', 'Bruno'];
    expect(enqueue()).toEqual()
  });
});

